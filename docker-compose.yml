services:
  nginx:
    image: nginx:alpine
    ports:
      - "9999:9999"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      api1:
        condition: service_healthy
      api2:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: "0.10"
          memory: "20MB"

  api1:
    image: andre09soares/rinha-backend-2026-dotnet:v3
    environment:
      - DOTNET_GCConserveMemory=9
    healthcheck:
      test:
        ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:9999/ready"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 120s
    deploy:
      resources:
        limits:
          cpus: "0.45"
          memory: "165MB"

  api2:
    image: andre09soares/rinha-backend-2026-dotnet:v3
    environment:
      - DOTNET_GCConserveMemory=9
    healthcheck:
      test:
        ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:9999/ready"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 120s
    deploy:
      resources:
        limits:
          cpus: "0.45"
          memory: "165MB"
